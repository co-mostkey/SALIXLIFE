# SALIX Life 환경변수 설정 예시
# 이 파일을 .env.local로 복사하여 사용하세요

# 배포 환경
NODE_ENV=production

# 이메일 전송 설정 (선택사항)
# 운영 환경에서는 아래 중 하나를 선택하여 설정하세요

# SMTP 설정 (Gmail 예시)
# SMTP_HOST=smtp.gmail.com
# SMTP_PORT=587
# SMTP_USER=your-email@gmail.com
# SMTP_PASS=your-app-password
# SMTP_FROM=noreply@salixlife.com

# SendGrid API (권장)
# SENDGRID_API_KEY=your-sendgrid-api-key

# AWS SES (엔터프라이즈)
# AWS_ACCESS_KEY_ID=your-aws-access-key
# AWS_SECRET_ACCESS_KEY=your-aws-secret-key
# AWS_REGION=us-east-1

# 연구게시판 이메일 인증 설정 (필수)
# JWT 시크릿 키 - 운영 환경에서는 반드시 강력한 랜덤 문자열로 변경하세요
JWT_SECRET=your-super-secret-jwt-key-change-this-in-production

# 인증 코드 유효 시간 (분 단위, 기본값: 5분)
AUTH_CODE_EXPIRY_MINUTES=5

# 관리자 이메일 목록 (쉼표로 구분)
ADMIN_EMAILS=admin@salix.co.kr,research@salix.co.kr,dev@salix.co.kr

# 포트 설정 (기본값: 3000)
# PORT=3000

# 데이터베이스 설정 (파일 기반이므로 별도 설정 불필요)
# 업로드 파일 저장소 경로
# UPLOAD_PATH=./public/uploads

# 보안 설정
# CORS_ORIGIN=https://your-domain.com
# ALLOWED_ORIGINS=https://your-domain.com,https://www.your-domain.com

# 로그 레벨 설정
# LOG_LEVEL=info

# 성능 모니터링 (선택사항)
# SENTRY_DSN=your-sentry-dsn
# ANALYTICS_ID=your-analytics-id 